/**
 * Application version - single source of truth
 * Update this value when releasing a new version.
 * Keep in sync with package.json.
 */
export const VERSION = '0.80.0';
export const APP_NAME = 'raynet-mcp-server';
